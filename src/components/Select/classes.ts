const trigger = 'flex items-center justify-center w-auto px-4 py-2 border border-transparent bg-white rounded-md cursor-pointer'
const triggerFocus = 'focus:outline-none focus-visible:outline-none'
const triggerHover = 'hover:text-secondary'

const portal = 'absolute left-0 mt-1 bg-main text-white rounded-md overflow-hidden shadow-lg z-50 w-auto'

const viewPort = 'p-3 text-white cursor-pointer  transition-all'
const viewPortFocus = 'focus:outline-none focus-visible:outline-none'
const viewPortHover = 'hover:bg-secondary'

export {
    trigger,
    triggerFocus,
    triggerHover,
    portal,
    viewPort,
    viewPortFocus,
    viewPortHover
}